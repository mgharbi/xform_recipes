#ifndef CONST_H_MMNUP4XP
#define CONST_H_MMNUP4XP

const int n_bins = 200;
const int inverse_oversampling = 10;
const int n_inverse_points = n_bins*inverse_oversampling;
const float normalized_inverse_delta = (1.0 / (n_inverse_points-1));

#endif /* end of include guard: CONST_H_MMNUP4XP */

